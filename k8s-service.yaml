apiVersion: v1
kind: Service
metadata:
  name: publish-management-service
spec:
  type: NodePort
  selector:
    app: publish-management
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30000